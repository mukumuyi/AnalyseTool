# <スクリプト名> 説明資料

- 対応スクリプト: `scripts/<スクリプト名>.py`
- 作成日 / 更新日: YYYY-MM-DD

## 処理概要

何のためのスクリプトで、どういう処理をするかを2〜5行程度で。
（例: 〇〇システムから出力されたCSVを読み込み、△△の条件で集計し、
月次サマリをExcelに出力する。）

## I/O説明

### 入力

| 項目 | 内容 |
| --- | --- |
| ファイル/引数 | パス・形式（CSV, Parquet, DBテーブル名など） |
| 必須カラム・スキーマ | 列名・型・意味 |
| 前提条件 | 事前に必要な状態（例: 特定ディレクトリにデータが置かれている 等） |

### 出力

| 項目 | 内容 |
| --- | --- |
| ファイル | 出力先パス・形式 |
| 内容 | 出力される列・集計内容 |
| 副作用 | ファイル上書きの有無、DBへの書き込み等 |

## 実行オプション

```bash
uv run python scripts/<スクリプト名>.py [オプション]
```

| オプション | 既定値 | 説明 |
| --- | --- | --- |
| `--xxx` | `yyy` | 説明 |

## 既知の制約・注意点（任意）

- 大きいデータでのメモリ使用量、前提とするデータ分布など、使う人が
  誤用しないために残しておきたい注意点があれば書く。
